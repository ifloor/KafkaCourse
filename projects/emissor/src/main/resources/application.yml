
server:
  port: 5051

spring:
  kafka:
    consumer:
      auto-offset-reset: earliest
      group-id: emissor
      bootstrap-servers: localhost:9092
    listener:
      missing-topics-fatal: false
    producer:
      bootstrap-servers: localhost:9092

kafka:
  schema-registry-server: http://localhost:8081